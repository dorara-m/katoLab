doctype html
html
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width')

    block title
      title ぽよ
    
    block styles
      link(rel='stylesheet', href='../styles/pages/about.css')
  body
    .wrapper
      img.wrapper__backImg(src='../images/apple-white.png')
      nav.left
        a(href='../index.html').left__title
          h1 KATO LAB.
          p 加藤研究室
        ul.left__menu
          include data/_pages
          each item in pages
            li 
              h2
                block menuLink
                  a(href=`../${item.link}`)=item.name
      main.main
        block content