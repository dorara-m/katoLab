doctype html
html
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width')

    block title
      title ぽよ
    
    block styles
      link(rel='stylesheet', href='/styles/pages/about.css')
  body
    .l-wrapper
      header.l-header
        .l-header__title.u-show
          h1: a(href="../index.html") KATO LAB.
          p 心理とスポーツとIT研究室
        nav.l-header__menu: ul
          include ../data/_pages
          each item in pages
            li: h2
              block menuLink
                a(href=`/${item.link}` class=item.name)=item.name
      main.main
        img(src='/images/apple-white.png').bgImg.u-show
        block content
      footer.l-footer
        a(href='/'): img.l-footer__img(src='/images/apple-black.png')
        .copyright: p Created by MM